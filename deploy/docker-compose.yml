# Compatibility entry point so `docker compose ...` in this directory uses the
# reviewed production stack. Docker Compose 2.20+ supports `include`.
name: engineering-notes

include:
  - path: compose.prod.yml
